// Example: Show an alert on clicking 'Order Now'
document.querySelectorAll('.btn-primary').forEach(button => {
    button.addEventListener('click', function() {
        alert('actually I did connected');
    });
});